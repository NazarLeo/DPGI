<Window x:Class="lab2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextEditor" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ВЕРХНЯ ПАНЕЛЬ (Рядок 0) -->
        <Border Grid.Row="0" BorderBrush="#FFCBBEBE" BorderThickness="2" CornerRadius="10" Margin="5">
            <StackPanel Orientation="Horizontal">
                <Button ToolTip="Відкрити" Margin="2">
                    <Image Source="images/open.png" Height="40" Width="40"/>
                </Button>
                <Button ToolTip="Стерти" Margin="2">
                    <Image Source="images/clear.png" Height="40" Width="40"/>
                </Button>
                <Button ToolTip="Копіювати" Margin="2">
                    <Image Source="images/copy.png" Height="40" Width="40"/>
                </Button>
                <Button ToolTip="Вставити" Margin="2">
                    <Image Source="images/paste.png" Height="40" Width="40"/>
                </Button>
            </StackPanel>
        </Border>

        <!-- НИЖНЯ ПАНЕЛЬ (Рядок 1) -->
        <Border Grid.Row="1" BorderBrush="#FFCBBEBE" BorderThickness="2" CornerRadius="10" Margin="5">
            <!-- Вкладений Grid із двома рядками та Margin -->
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <!-- Текстова область займає все місце -->
                    <RowDefinition Height="Auto"/>
                    <!-- Рядок для кнопки підлаштовується автоматично -->
                </Grid.RowDefinitions>

                <!-- TextBox з вертикальною прокруткою -->
                <TextBox Grid.Row="0" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto"/>

                <!-- DockPanel для позиціонування кнопки -->
                <DockPanel Grid.Row="1" LastChildFill="False">
                    <Button Content="OK" 
                            DockPanel.Dock="Bottom" 
                            HorizontalAlignment="Center" 
                            Padding="10,2" 
                            Margin="0,10,0,0"/>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</Window>